# خطة شاملة ومتكاملة لتحصين وتطوير مستودع الباك اند
## Accounting System Backend — من مستوى جيد إلى مستوى احترافي عالمي

> هذه الخطة مبنية على واقع المشروع الحالي: NestJS + Prisma + PostgreSQL + pnpm + Docker، بـ 15 وحدة (Company, Users, Auth, Products, Inventory, Sales & POS, Purchases, Accounting, Reports, Printing, WhatsApp, Sync, Audit, Settings, Health)، ومستودعين إضافيين يعتمدان عليه (Android, Windows).
>
> الهدف: جعل هذا الباك اند **المصدر الوحيد للحقيقة (Single Source of Truth)** الموثوق والمحصّن الذي يبني عليه العميلان بثقة كاملة.

---

## المبدأ العام للخطة

كل مرحلة تُبنى فوق التي قبلها. **لا تنتقل لمرحلة تالية قبل إغلاق المرحلة الحالية فعليًا** (checklist مكتمل، ليس فقط "شغال عندي"). هذا هو الفرق بين مشروع هاوٍ ومشروع احترافي.

---

## المرحلة 0 — التنظيف والتأسيس (Housekeeping)
**المدة المقدرة: 1-2 يوم | الهدف: إزالة كل ما يعطي انطباعًا غير احترافي ويُهيئ الأرضية**

### الخطوات
1. حذف الملف الخاطئ `docker-compose.yml,range:[],text:` من المستودع نهائيًا.
2. إزالة مجلد `dist` من التتبع في git وإضافته إلى `.gitignore`:
   ```
   dist/
   node_modules/
   *.log
   .env
   .env.*.local
   coverage/
   ```
3. مراجعة `pnpm-workspace.yaml` — تأكيد أنه مقصود (monorepo فعلي) أو حذفه إن كان بقايا إعداد أولي.
4. تأكيد عدم وجود أي ملف `.env` حقيقي مرفوع بالخطأ في تاريخ commits سابقة (فحص باستخدام `git log --all --full-history -- .env`).
5. إن وُجد أي secret مسرّب في التاريخ، يجب اعتباره مخترقًا فورًا: تدويره (rotate) بالكامل + تنظيف التاريخ (`git filter-repo` أو BFG).
6. إنشاء `CHANGELOG.md` وبدء توثيق كل تغيير من الآن فصاعدًا (Keep a Changelog format).
7. تفعيل قواعد حماية الفرع الرئيسي (Branch Protection) على `main`:
   - منع الدفع المباشر (direct push)
   - إلزامية Pull Request + مراجعة واحدة على الأقل
   - إلزامية اجتياز فحوصات CI قبل الدمج

### معيار إغلاق المرحلة
- [ ] لا يوجد أي ملف غريب أو build artifact في الجذر
- [ ] `.gitignore` شامل ومُختبر
- [ ] `main` محمي بالكامل

---

## المرحلة 1 — تحصين الأمان (Security Hardening)
**المدة المقدرة: 1-2 أسبوع | الأولوية القصوى — هذا نظام محاسبي يحوي بيانات مالية حساسة لعدة شركات**

### 1.1 المصادقة والتفويض (Auth & Authorization)
1. مراجعة تطبيق JWT الحالي: التأكد من استخدام access token قصير العمر (15-30 دقيقة) + refresh token طويل العمر مخزّن بشكل آمن.
2. تطبيق آلية إبطال (revocation) للـ refresh tokens عند تسجيل الخروج أو الاشتباه بخرق.
3. بناء نظام RBAC/Permissions دقيق على مستوى كل endpoint وليس فقط على مستوى الدور العام — مثال: صلاحية `sales:invoice:void` منفصلة عن `sales:invoice:create`.
4. اختبار Guards على **كل** route بلا استثناء (لا يوجد route منسي بلا حماية).
5. تفعيل قفل الحساب المؤقت بعد محاولات دخول فاشلة متكررة (Brute-force protection).

### 1.2 عزل البيانات متعددة الشركات (Multi-Tenancy Isolation)
هذه أهم نقطة أمان في مشروعك تحديدًا:
1. مراجعة كل query في كل service للتأكد من أن `tenantId` مُطبّق دائمًا (لا استثناءات).
2. كتابة اختبارات تلقائية مخصصة: "مستخدم شركة A يحاول الوصول لبيانات شركة B" على كل موديول (Products, Sales, Accounting...) ويجب أن تفشل كل محاولة.
3. التفكير في تطبيق Row-Level Security على مستوى قاعدة البيانات كطبقة حماية إضافية (دفاع متعدد الطبقات، ليس فقط منطق التطبيق).

### 1.3 التحقق من المدخلات (Input Validation)
1. تفعيل `ValidationPipe` بشكل عام (global) في `main.ts` بالإعدادات التالية:
   - `whitelist: true`
   - `forbidNonWhitelisted: true`
   - `transform: true`
2. كل DTO يجب أن يستخدم `class-validator` decorators بدقة (لا حقول بلا تحقق).
3. حماية خاصة لحقول الأسعار والكميات المالية من القيم السالبة أو الفاسدة (خصوصًا في Accounting و Sales و Purchases).

### 1.4 حماية الشبكة والهيدرز
1. مراجعة إعدادات `Helmet` والتأكد من تفعيل Content-Security-Policy فعليًا وليس بالإعدادات الافتراضية فقط.
2. ضبط CORS بدقة: قائمة origins محددة (Android app + Windows app + أي frontend لاحق)، لا `*` أبدًا في الإنتاج.
3. تفعيل HSTS وفرض HTTPS بالكامل خلف الـ reverse proxy.
4. مراجعة Rate Limiting الحالي وتخصيصه لكل نوع endpoint:
   - `/auth/login` → محدود جدًا (مثلاً 5 محاولات/دقيقة لكل IP)
   - endpoints القراءة العامة → أوسع
   - endpoints الكتابة الحساسة (فواتير، قيود محاسبية) → محدود ومراقب

### 1.5 إدارة الأسرار (Secrets Management)
1. التأكد أن جميع المفاتيح (JWT secret, DB credentials, WhatsApp API keys) تأتي من environment variables فقط.
2. الانتقال تدريجيًا نحو حل إدارة أسرار مركزي (Doppler / AWS Secrets Manager / HashiCorp Vault) بدل ملفات `.env` يدوية في الإنتاج.
3. تدوير (rotate) كل الأسرار الحالية فور الانتهاء من هذه المرحلة (احتياطًا لأي تسريب سابق محتمل).

### 1.6 الفحص الآلي المستمر
1. تفعيل **Dependabot** أو **Snyk** لفحص الثغرات في التبعيات تلقائيًا.
2. تفعيل **Gitleaks** أو **TruffleHog** ضمن CI لفحص أي secret يُدفع بالخطأ قبل الدمج.
3. تشغيل `pnpm audit` بشكل دوري وربطه بـ CI كخطوة فاشلة عند وجود ثغرات حرجة.

### 1.7 سجل التدقيق (Audit Log) — تعزيز الموديول الموجود
1. التأكد أن كل عملية حساسة (حذف فاتورة، تعديل قيد محاسبي، تغيير صلاحيات مستخدم) تُسجَّل مع: من نفّذها، متى، من أي IP/جهاز، والقيمة قبل وبعد التغيير.
2. جعل سجل التدقيق **غير قابل للتعديل** من داخل التطبيق نفسه (append-only).

### معيار إغلاق المرحلة
- [ ] لا يوجد endpoint واحد بلا حماية مناسبة
- [ ] اختبارات عزل tenant تمر 100%
- [ ] لا أسرار داخل الكود أو تاريخ git
- [ ] فحص الثغرات آلي ويعمل في كل PR

---

## المرحلة 2 — جودة البنية والكود (Architecture & Code Quality)
**المدة المقدرة: 1-2 أسبوع**

1. توحيد نمط الاستجابة (Response envelope) لكل الـ API: شكل موحّد للنجاح والخطأ عبر كل الموديولات.
   ```json
   { "success": true, "data": {...}, "meta": {...} }
   { "success": false, "error": { "code": "VALIDATION_ERROR", "message": "...", "details": [...] } }
   ```
2. بناء `Global Exception Filter` موحّد يترجم كل الأخطاء (Prisma errors, validation errors, business errors) لنفس الشكل أعلاه.
3. تعريف أكواد أخطاء ثابتة وموثقة (Error Codes Dictionary) — هذا ضروري جدًا لأن تطبيقي الأندرويد والويندوز سيبنيان منطقهما عليها.
4. مراجعة `ConfigModule`: التحقق من كل متغيرات البيئة عبر schema validation (Joi أو Zod) بحيث يفشل تشغيل السيرفر فورًا برسالة واضحة لو أي متغير ناقص أو خاطئ، بدل فشل غامض لاحقًا.
5. تطبيق Logging منظم (Pino مثلاً) مع:
   - Correlation ID / Request ID لكل طلب لتتبعه عبر كل الموديولات
   - عدم تسجيل بيانات حساسة (كلمات مرور، توكنات) في اللوقز أبدًا
6. مراجعة مخطط Prisma (`schema.prisma`):
   - التأكد من وجود indexes على الحقول المستخدمة في البحث والفلترة (خصوصًا `tenantId`)
   - مراجعة استراتيجية الحذف (soft delete مقابل hard delete) وتوحيدها عبر كل الجداول
   - مراجعة العلاقات (relations) والقيود (constraints) للتأكد من سلامة البيانات على مستوى قاعدة البيانات نفسها
7. توثيق كل API عبر Swagger/OpenAPI بشكل كامل (`@ApiProperty`, `@ApiOperation` على كل endpoint) — هذا سيصبح **العقد الرسمي** بينك وبين مطوري الأندرويد والويندوز.

### معيار إغلاق المرحلة
- [ ] كل الأخطاء تخرج بنفس الشكل الموحّد
- [ ] Swagger يعرض 100% من الـ endpoints بدقة
- [ ] لا يوجد أي console.log متبقٍ في الكود، فقط logger منظم

---

## المرحلة 3 — الاختبار الشامل (Testing & QA)
**المدة المقدرة: 1-2 أسبوع**

1. رفع تغطية Unit Tests لكل service (المنطق التجاري)، ليس فقط الاعتماد على E2E الحالية.
2. التركيز الخاص على اختبار **المحرك المحاسبي**: كل قيد يجب أن يوازن (مدين = دائن) دائمًا، واختبار كل حالة حافة (edge case) في الحسابات.
3. اختبار شامل لمحرك المزامنة (Sync): تعارض البيانات (conflict resolution)، تكرار الطلبات (idempotency)، فقدان الاتصال أثناء العملية.
4. تحديد عتبة تغطية إلزامية في CI (مثلاً 75-80%) بحيث يُرفض أي PR ينزل تحتها.
5. اختبار حمل (Load Testing) باستخدام k6 أو Artillery على:
   - نقاط البيع (Sales/POS) في وقت الذروة
   - محرك المزامنة عند رجوع عدة أجهزة أوفلاين دفعة واحدة
6. اختبار الأمان الآلي (مثل OWASP ZAP baseline scan) كخطوة دورية.

### معيار إغلاق المرحلة
- [ ] تغطية اختبارات ≥ 75%
- [ ] المحرك المحاسبي مغطّى 100% باختبارات edge cases
- [ ] نتائج اختبار الحمل موثقة ومقبولة

---

## المرحلة 4 — CI/CD والبنية التحتية (DevOps)
**المدة المقدرة: 1 أسبوع**

1. بناء GitHub Actions pipeline كامل يعمل على كل PR:
   - `lint` → `test` (unit + e2e) → `build` → `docker build`
   - فشل أي خطوة يمنع الدمج تلقائيًا
2. تحويل `Dockerfile` إلى multi-stage build لتصغير حجم الصورة النهائية وتسريع النشر.
3. إعداد بيئة **Staging** منفصلة تمامًا عن Production لاختبار كل تحديث قبل النشر الفعلي.
4. إضافة health checks حقيقية (liveness/readiness probes) تُستخدم من قِبل أي orchestrator (Docker Swarm/Kubernetes مستقبلاً).
5. استراتيجية النشر: Blue-Green أو Rolling deployment لتفادي أي توقف عند التحديث.
6. خطة نسخ احتياطي لقاعدة البيانات:
   - نسخ يومي تلقائي + نسخ قبل كل migration حساس
   - اختبار دوري فعلي لاستعادة النسخة الاحتياطية (نسخة لا تُختبر = نسخة غير موجودة)
7. ربط أداة مراقبة أخطاء (Sentry) وأداة مراقبة أداء (Prometheus + Grafana أو بديل SaaS).

### معيار إغلاق المرحلة
- [ ] لا نشر يدوي — كل شيء عبر pipeline
- [ ] استعادة النسخة الاحتياطية مُختبرة فعليًا مرة واحدة على الأقل
- [ ] تنبيهات فورية تصل عند أي خطأ حرج في الإنتاج

---

## المرحلة 5 — التوثيق والحوكمة (Documentation & Governance)
**المدة المقدرة: 3-5 أيام**

1. إعادة كتابة `README.md` بمستوى احترافي: badges لحالة البناء والتغطية، شرح معماري مختصر، روابط توثيق.
2. إنشاء `SECURITY.md`: سياسة واضحة للإبلاغ عن الثغرات الأمنية (ضروري جدًا لمشروع مالي).
3. إنشاء `CONTRIBUTING.md`: معايير كتابة الكود، نمط commits، عملية مراجعة الـ PR.
4. توثيق قرارات العمارة المهمة عبر ADR (Architecture Decision Records) — مثال: "لماذا اخترنا Prisma"، "استراتيجية المزامنة Offline-First".
5. تصدير OpenAPI schema بشكل رسمي (`openapi.json`) ونشره كملف قابل للاستيراد المباشر في Postman/Insomnia.
6. توضيح ملف الترخيص (License) بدقة — مهم جدًا إذا كان المشروع تجاريًا مغلق المصدر.

### معيار إغلاق المرحلة
- [ ] أي مطور جديد يقدر يفهم المشروع ويشغّله من التوثيق فقط، بدون سؤالك
- [ ] `openapi.json` منشور ومحدَّث تلقائيًا مع كل نشر

---

## المرحلة 6 — الربط الاحترافي مع مستودعي الأندرويد والويندوز
**المدة المقدرة: مستمرة بعد الأساس**

1. توليد SDK/Client تلقائي من `openapi.json` لكل من الأندرويد (Kotlin/Retrofit) والويندوز (C#/HttpClient) بدل الاستدعاءات اليدوية — يقلل الأخطاء البشرية بشكل كبير.
2. وضع استراتيجية توافق إصدارات (Versioning Strategy) واضحة بين نسخة السيرفر ونسخ العملاء، خصوصًا مع اعتماد النظام على Offline-First Sync (عميل بنسخة قديمة يجب ألا يكسر البيانات).
3. إنشاء بيئة تكامل مشتركة (Shared Staging) يختبر عليها الفريقان (أندرويد + ويندوز) قبل أي نشر رسمي.
4. توثيق "عقد المزامنة" (Sync Contract) بدقة: شكل كل عملية Push/Pull، وكيفية حل التعارضات، بلغة يفهمها مطورا العميلين مباشرة دون الرجوع لك في كل مرة.

### معيار إغلاق المرحلة
- [ ] لا يوجد استدعاء API واحد في الأندرويد أو الويندوز غير موثّق في العقد الرسمي
- [ ] تحديث الباك اند لا يكسر أي عميل قديم متصل بدون تنبيه مسبق (Deprecation policy)

---

## الجدول الزمني التقديري الإجمالي

| المرحلة | المدة | الأولوية |
|---|---|---|
| 0 — التنظيف | 1-2 يوم | فوري |
| 1 — الأمان | 1-2 أسبوع | الأعلى |
| 2 — جودة الكود | 1-2 أسبوع | عالية |
| 3 — الاختبار | 1-2 أسبوع | عالية |
| 4 — CI/CD | 1 أسبوع | عالية |
| 5 — التوثيق | 3-5 أيام | متوسطة |
| 6 — الربط مع العملاء | مستمرة | متوسطة → مستمرة |

**الإجمالي التقريبي للأساس المتين: 5-7 أسابيع عمل مركّز**، بعدها يصبح المستودع بمستوى احترافي جاهز لتحمّل نمو حقيقي وربط آمن مع تطبيقي الأندرويد والويندوز.

---

## قاعدة ذهبية للطريق كله

لا تنتقل من مرحلة إلى التي تليها إلا بعد أن يمر **checklist** المرحلة الحالية بالكامل. الانضباط في الترتيب هو الفرق الحقيقي بين "مستودع فيه ميزات كثيرة" و"مستودع احترافي موثوق فعلاً" — وهذا بالضبط ما تسعى إليه.
